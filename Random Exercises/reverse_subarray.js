/*–Given an array of numbers. You need to find if reversing a subarray can sort the array. For example –
Input – [1,2,5,4,3]
Output – true (reversing {5,4,3} will sort the array)*/

function checkSort(arr){
    let n = arr.length-1;
    for(let i = 0, j=1; i < n; i++,j++){
    
    if(arr[j]-arr[i]==1){
        
    }
        }
    }
}